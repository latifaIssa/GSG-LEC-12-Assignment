import 'package:flutter/material.dart';

class MershantPage extends StatelessWidget {
  // const MershantPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Mershant'),
    );
  }
}
